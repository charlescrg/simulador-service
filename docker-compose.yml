services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: sqlserver-hack
    ports:
      - "1433:1433"
    environment:
      ACCEPT_EULA: "Y"
      SA_PASSWORD: "Caixa#01"
    volumes:
      - ./init-db:/docker-entrypoint-initdb.d
    restart: always
